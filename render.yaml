services:
  - type: worker
    name: telegram-cash-bot
    runtime: python
    buildCommand: |
      python -m venv venv
      source venv/bin/activate
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: |
      source venv/bin/activate
      python bot.py
    plan: free


